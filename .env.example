# Database Configuration
# Railway automatically provides DATABASE_URL when you add PostgreSQL
# Example: postgresql://postgres:password@host:port/database
DATABASE_URL=postgresql://user:password@localhost:5432/vibe_api

# Server Configuration
# Railway automatically provides PORT (do not set manually)
# PORT=3000

# JWT Authentication
# REQUIRED: Generate a secure random string (minimum 32 characters)
# You can generate one with: openssl rand -base64 32
JWT_SECRET=your-super-secret-key-minimum-32-characters-long-replace-this

# Environment
# Set to "production" for Railway deployment
ENVIRONMENT=production

# Logging
# Recommended settings for production
RUST_LOG=info,vibe_api=debug

# Optional: OpenAI Integration
# Only required if using AI features
# OPENAI_API_KEY=sk-...

# Optional: Anthropic Integration
# Only required if using AI features
# ANTHROPIC_API_KEY=sk-ant-...

# Optional: S3 Storage
# Only required if using file storage features
# S3_BUCKET=your-bucket-name
# S3_REGION=us-east-1
# S3_ACCESS_KEY=your-access-key
# S3_SECRET_KEY=your-secret-key
# S3_ENDPOINT=https://s3.amazonaws.com

# Optional: Application Settings
# APP_NAME=Flow API
# APP_VERSION=0.1.0
# MAX_UPLOAD_SIZE=10485760
# CORS_ORIGINS=*
